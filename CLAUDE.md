@AGENTS.md

# このリポジトリの進め方

このリポジトリは gym-buddy と同じ作りかた・同じ構成にしてあります。

## 変更は main に直接反映してよい

このリポジトリのオーナーから、**作業ブランチやプルリクエストを経由せず、
main に直接プッシュしてよい**という許可が出ています(2026-09-06)。
gym-buddy と同じ扱いです。個人用のアプリで、ほかにレビューする人が
いないためです。

- セッション開始時に作業用ブランチを割り当てられた場合でも、作業が終わったら
  **必ず main に反映してから終わること**(main に直接プッシュするか、
  作業ブランチを main にマージしてプッシュする)。
  作業ブランチに置いたまま「マージしてください」と頼んで終わらないこと
- プルリクエストは、オーナーから明示的に頼まれたときだけ作ること

## main に反映する前に必ず次を通すこと

main への反映は、そのまま**本番(Vercel)への公開**と、
**本番データベースへのマイグレーション適用**を意味します。
取り消しが効きにくいので、プッシュ前にローカルで次の 3 つを必ず通すこと。

```bash
npm run lint   # 書き方のチェック
npm test       # ユニットテスト
npm run build  # 本番と同じビルド(型エラーもここで出る)
```

1 つでも落ちる状態でプッシュしないこと(作業ブランチでも同じ)。

DB マイグレーションを追加した場合は、それに加えて**ローカルの PostgreSQL に
Phase 1 から通しで適用**し、2 回流しても壊れないこと(冪等)を確認すること。
本番の DB には実際の買い物の記録が入っているので、データを消す変更は入れないこと。

## 高い/安いの判定を触るときの注意

このアプリの値打ちは「その値段が高いのか安いのか」を正しく言えることに尽きます。
判定のしくみは `lib/priceStats.ts` に集めてあり、
`lib/priceStats.test.ts` でテストしてあります。

- しきい値や相場の出し方を変えるときは、**必ずテストも一緒に直すこと**。
  テストが「なぜその作りなのか」の説明になっています
  (特売を相場から外す、平均ではなく中央値を使う、など)。
- 判定のロジックを画面(`app/`)の中に書かないこと。
  同じ判定が複数の画面から使われるので、`lib/` に置いてテストできる形に保つこと。

## プッシュしたあとの確認

main への反映で GitHub Actions が 2 つ動きます。
**どちらも成功したことを確認してから作業を終えること。**

| ワークフロー | 何をするか |
| --- | --- |
| `ci.yml` | lint・テスト・ビルド |
| `supabase-migrations.yml` | 本番 DB にマイグレーションを適用 |

失敗していたら、その場で直すか、直せない場合は何が失敗したのかを
オーナーに具体的に伝えること。
